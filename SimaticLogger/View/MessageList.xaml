<UserControl x:Class="SimaticLogger.View.MessageList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimaticLogger.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="messagesTemplate">
            <Grid Margin="0.5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Margin="5" FontSize="15" Text="{ Binding Text }" Grid.Column="1"/>
                <TextBlock Margin="5" FontSize="15" Text="{ Binding Date }" Grid.Column="0"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListBox Grid.Row="1" Name="messages" ItemTemplate="{DynamicResource messagesTemplate}" ItemsSource="{Binding Messages}"/>
    </Grid>
</UserControl>
